# Outputs Root

`OUTPUTS/` is the configured output root for the `GBOGEB_FEDERATION` workspace.

Use this directory for generated workspace outputs, exports, bundles, and transient handover artifacts. Generated outputs should retain links back to their source evidence and canonical governance references.
