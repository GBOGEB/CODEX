schema: "theme_map.v1"
description: "Maps tuple themes to normalized runtime domains."
themes:
  RUNTIME_GOVERNANCE:
    domain: "runtime"
    keywords:
      - governance
      - policy
      - routing
  CHAT_TUPLE:
    domain: "ingress"
    keywords:
      - tuple
      - session
      - chat
  INDEX_GENERATION:
    domain: "publishing"
    keywords:
      - markdown
      - jekyll
      - index
