delta1_topology_freeze:
  moniker: DELTA_1

  freeze_domains:
    - governance_freeze
    - runtime_freeze
    - release_freeze
    - continuity_freeze

  freeze_requirements:
    recursive_topology_normalization: enabled
    semantic_governance_consistency: enabled
    federated_release_stability: enabled

  release_targets:
    - DELTA_1_RC1
    - v0.1.0-delta1

  objectives:
    - topology_stabilization
    - governance_release_consistency
    - operational_runtime_alignment
