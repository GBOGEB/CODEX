abacus_governance_interface_manifest:
  interface_version: delta_1

  governance_interfaces:
    - recursive_policy_engine
    - semantic_validation_engine
    - operational_provenance_engine
    - adaptive_execution_alignment

  interoperability:
    abacus_integration: enabled
    recursive_governance_sync: enabled
    semantic_runtime_visibility: enabled

  lifecycle:
    validation_required: true
    synchronization_required: true
    provenance_required: true
