# W62 Cross-Repo P1→P6 DoV — Historical Snapshot

**Status:** historical cross-repo handover / DoV snapshot; currentness check required.  
**Primary raw provenance:** `Repo ARchitecture Breakdown 0809.txt`, Library file `file_00000000d5508210b4f4a53b2efc5d2c`.  
**Parsed-text alias:** `ALAT SSOT 0809.txt`, Library file `file_00000000cedc81f48a1bc5b8d85e5aa3`, contains the same parsed 51-line content despite its misleading filename.  
**Raw SHA256:** not bound: Library raw materialization returned 403 for both files. Do not substitute a hash of extracted text for the unavailable raw bytes.

## Context

This snapshot records a cross-repository handover state around cryoplant, CODEX and ABACUS. Its central distinction is still valuable: the session could close as a controlled handover while terminal cross-repository DoV remained withheld.

The source is not ALAT offer evidence despite one alias filename suggesting that. Its actual subject is GitHub roundtrip topology, execution proof, reproducibility and release/publication gates.

## Historical observed state

The snapshot recorded:

- formal engineering closure at `70/90 = 77.78%`;
- negotiation closure at `0/20`;
- A-item engineering closure `0/14 CLOSED`, with 5 RED and 9 AMBER;
- binary/offer-evaluation DoV officially promoted at `40/100`, with later progression still conditional;
- W62 tuple propagation structurally present across three repos;
- P1 exact-head green at `0/3` repos;
- raw workflow ratio `27/37 ≈ 73%`, diagnostic only;
- P2 fresh-checkout reproducibility at `0/3` governed PASS receipts;
- P3/P4/P5/P6 staged structurally, but not independently sufficient for terminal promotion;
- all three main branches unprotected at that historical point;
- protected tag/release smoke still open.

## Strongest completed element

The source identifies the **semantic roundtrip topology** as the strongest completed part:

`child send → CODEX/KEB forward/normalization → ABACUS/DOW return → CODEX normalization → child re-entry candidate`.

It reports exact tuple/digest preservation and zero-delta fields in the staged parent/child receipts. The architectural path existed; the missing proof was execution/reproducibility.

## Historical blockers

Two review findings prevented premature closure:

1. **CODEX semantic-vocabulary / hashing concern** — `CENSUS_DOV_PROOF` was being routed before the operation was formally registered, and raw-file SHA stability on Windows was challenged because line endings were not pinned.
2. **ABACUS reproducibility concern** — the deterministic method named by the receipt was text-only; no executable fresh-checkout validator existed to reproduce the claimed PASS.

These are useful lineage findings, not assertions about current main.

## P1→P6 execution chain recorded

### P1 — exact-SHA green
Repair the actual control-plane/startup/workflow residuals until all three governed heads are green on the required gate set.

### P2 — reproducibility
Implement an executable roundtrip validator, run it from fresh checkout in each repo, and canonicalize hashing/line-ending behavior.

### P3 — parity
Re-run the same immutable tuple across all three repos and prove generated output/hash parity rather than relying on static declarations.

### P4 — typed parent receipts
Accept KEB/DOW receipts only when generated by the executable validator.

### P5 — child re-entry
Let cryoplant consume accepted parent receipts and commit a governed `ACCEPT / REJECT / DEFER` disposition.

### P6 — publication
Repeat zero-delta from fresh checkout, bind protected/green SHAs, tag/release, download and smoke the published artifact, then record the final publication receipt.

## Historical restart spine

The source records the restart path as:

`cryoplant #719 → CODEX #526 → ABACUS #1032 → CODEX normalization → cryoplant #719 re-entry`.

This is historical navigation only. Current PR numbers, branch protection, workflow ratios and blocker states must be refreshed before operational use.

## Closure semantics retained

The historical conclusion was:

- **Session DoD:** achieved;
- **Session DoV:** achieved for controlled handover and GitHub roundtrip capture;
- **Terminal cross-repo DoV:** withheld;
- **Formal engineering:** unchanged at 70/90.

That separation remains a useful governance pattern even when the underlying numbers are stale.

## Current-use rule

Use this document to understand why later federation work emphasized exact-head execution, fresh-checkout reproducibility, canonical hashing and non-compensating publication gates. Do not use its historical workflow counts or PR states as current truth.

## Provenance and authority boundary

This is a human editorial rewrite of a historical snapshot. Two differently named Library files expose the same parsed content; raw-byte identity could not be checked due Library materialization failure. The misleading `ALAT SSOT` alias is therefore retained only as provenance and must never be treated as ALAT evidence from this text.
