Run before committing the QPS subfolder:

```bash
python qps/cost-master/ci/check_public_source_only.py qps/cost-master
```

This is a subfolder policy. It does not claim that the entire historic CODEX
repository is binary-free.
