name: W05 QPS Roundtrip Regeneration Zero-Delta (legacy compatibility)

"on":
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  delegate-w69:
    name: delegate-to-canonical-w69
    uses: ./.github/workflows/w69-qps-zero-delta-execution.yml
