name: TRIAGE Repo Census
on:
  pull_request:
    paths: ['triage/census/**','scripts/validate_triage_census.py','.github/workflows/triage-repo-census.yml']
  push:
    branches: [main]
    paths: ['triage/census/**','scripts/validate_triage_census.py']
jobs:
  census:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install pyyaml
      - run: python scripts/validate_triage_census.py
      - name: Guard KEB authority boundary
        run: |
          grep -q 'KEB_SEMANTIC_CONTROL' triage/census/CODEX.census.yaml
          grep -q 'parent repo may not promote engineering facts' triage/census/CODEX.census.yaml
