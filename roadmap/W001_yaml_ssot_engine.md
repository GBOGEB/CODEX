# W001 YAML SSOT Engine

Define and validate the authoritative YAML SSOT contract data model.

## Exit criteria

- Validate `ssot/master_contract_ssot_v0_2.yaml` against `schemas/master_contract_ssot.schema.yaml`.
- Run validation in CI for SSOT, schema, and validator changes.
- Keep W000 draft until SSOT validation is green.
