# Jekyll static-render defaults for governance documentation

title: CODEX Runtime Governance
markdown: kramdown
theme: minima

defaults:
  - scope:
      path: "docs"
      type: "pages"
    values:
      layout: default
      toc: true
      runtime_plane: abacus_codex_federation

exclude:
  - outputs/
  - .github/
